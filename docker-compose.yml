services:
  n8n:
    image: n8nio/n8n
    ports:
      - "5678:5678"
    environment:
      - N8N_BASIC_AUTH_ACTIVE=false
      - N8N_ALLOW_EXEC=true
      - N8N_RUNNERS_ENABLED=true
    volumes:
      - ./:/workspace
      - ./n8n_data:/home/node/.n8n
    working_dir: /workspace